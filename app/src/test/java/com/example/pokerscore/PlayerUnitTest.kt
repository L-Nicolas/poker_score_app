package com.example.pokerscore

import com.example.pokerscore.card.Card
import com.example.pokerscore.card.CardPoker
import com.example.pokerscore.hand.Hand
import com.example.pokerscore.player.Player
import org.junit.Test

class PlayerUnitTest {
    /*@Test
    fun `should have a name`() {
        val listCardsPlayer1 = listOf(
            CardPoker(Card.Suit.CLUBS, Card.Figure.QUEEN),
            CardPoker(Card.Suit.HEARTS, Card.Figure.QUEEN),
        )
        val player = Player("John", 100, Hand(listCardsPlayer1))
        assert(player.name == "John")
    }

    @Test
    fun `should have a hand`() {
        val listCardsPlayer1 = listOf(
            CardPoker(Card.Suit.CLUBS, Card.Figure.QUEEN),
            CardPoker(Card.Suit.HEARTS, Card.Figure.QUEEN),
        )
        val player = Player("John", 100, Hand(listCardsPlayer1))
        assert(player.hand is Hand)
    }

    @Test
    fun `should have a chip count`() {
        val listCardsPlayer1 = listOf(
            CardPoker(Card.Suit.CLUBS, Card.Figure.QUEEN),
            CardPoker(Card.Suit.HEARTS, Card.Figure.QUEEN),
        )
        val player = Player("John", 100, Hand(listCardsPlayer1))
        assert(player.chipCount == 100)
    }*/
}